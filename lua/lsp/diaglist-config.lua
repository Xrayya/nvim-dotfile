-- local map = vim.api.nvim_set_keymap
-- local opts = { noremap = true, silent = true }

-- map('n', '<Leader>la', '<cmd>lua require(\'diaglist\').open_all_diagnostics()<cr>', opts)
-- map('n', '<Leader>lb', '<cmd>lua require(\'diaglist\').open_buffer_diagnostics()<cr>', opts)

--require('lspconfig').sumneko_lua.setup {
--  settings = {
--    Lua = {
--      diagnostics = {
--        globals = {'vim'}
--      }
--    }
--  }
--}
